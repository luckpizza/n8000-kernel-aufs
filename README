This repo has the kernel with all the configuration asked and support for aufs added

Steps to compile:

- Download the gcc toolchain
if mac OSX
- export CROSS_COMPILE=<PATH_TO_TOOLCHAIN>/prebuilt/darwin-x86/toolchain/arm-eabi-4.4.3/bin/arm-eabi-
if linux
- export CROSS_COMPILE=<PATH_TO_TOOLCHAIN>/prebuilt/linux-x86/toolchain/arm-eabi-4.4.3/bin/arm-eabi-

- export ARCH=arm

- make

DONE!
